import { TabsType } from "../../types/types";
export declare const Tabs: ({ activeTab, setActiveTab, }: {
    activeTab: string;
    setActiveTab: (newTab: TabsType) => void;
}) => import("react/jsx-runtime").JSX.Element;
